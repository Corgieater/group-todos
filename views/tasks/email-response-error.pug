doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1")
    title Link Expired - Task Management
    include ../partials/bootstrap
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css")
    style.
      body { background-color: #f8f9fa; }
      .error-card { max-width: 500px; margin: 80px auto; border: none; border-radius: 15px; }
      .error-icon { font-size: 4rem; color: #dc3545; }

  body
    .container
      .card.shadow.error-card
        .card-body.text-center.p-5
          .error-icon.mb-4
            i.bi.bi-exclamation-triangle-fill
          
          h1.h3.mb-3 Link Expired
          
          p.text-muted.mb-4 
            | Sorry, this response link has expired (links are valid for 24 hours) or the request is invalid.
          
          if error
            .alert.alert-danger.small.mb-4= error

          .d-grid.gap-2
            a.btn.btn-primary(href="/tasks/home") 
              i.bi.bi-house-door.me-2
              | Return to Home & Sign In
            
            a.btn.btn-outline-secondary.btn-sm(href="javascript:history.back()") Go Back

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js")